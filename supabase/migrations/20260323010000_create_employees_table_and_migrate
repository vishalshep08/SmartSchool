-- Fix student_leave_requests → teachers
ALTER TABLE student_leave_requests
  DROP CONSTRAINT IF EXISTS student_leave_requests_reviewed_by_teacher_id_fkey;

ALTER TABLE student_leave_requests
  ADD CONSTRAINT student_leave_requests_reviewed_by_teacher_id_fkey
  FOREIGN KEY (reviewed_by_teacher_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix homework → teachers
ALTER TABLE homework
  DROP CONSTRAINT IF EXISTS homework_assigned_by_fkey;

ALTER TABLE homework
  ADD CONSTRAINT homework_assigned_by_fkey
  FOREIGN KEY (assigned_by)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix whatsapp_logs → teachers
ALTER TABLE whatsapp_logs
  DROP CONSTRAINT IF EXISTS whatsapp_logs_teacher_id_fkey;

ALTER TABLE whatsapp_logs
  ADD CONSTRAINT whatsapp_logs_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix permission_change_log → teachers
ALTER TABLE permission_change_log
  DROP CONSTRAINT IF EXISTS permission_change_log_employee_id_fkey;

ALTER TABLE permission_change_log
  ADD CONSTRAINT permission_change_log_employee_id_fkey
  FOREIGN KEY (employee_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix document_requests → teachers (principal_employee_id)
ALTER TABLE document_requests
  DROP CONSTRAINT IF EXISTS document_requests_principal_employee_id_fkey;

ALTER TABLE document_requests
  ADD CONSTRAINT document_requests_principal_employee_id_fkey
  FOREIGN KEY (principal_employee_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix staff_permissions → teachers
ALTER TABLE staff_permissions
  DROP CONSTRAINT IF EXISTS staff_permissions_employee_id_fkey;

ALTER TABLE staff_permissions
  ADD CONSTRAINT staff_permissions_employee_id_fkey
  FOREIGN KEY (employee_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix fee_payments → teachers
ALTER TABLE fee_payments
  DROP CONSTRAINT IF EXISTS fee_payments_collected_by_employee_id_fkey;

ALTER TABLE fee_payments
  ADD CONSTRAINT fee_payments_collected_by_employee_id_fkey
  FOREIGN KEY (collected_by_employee_id)
  REFERENCES teachers(id)
  ON DELETE SET NULL;

-- Fix employee_emergency_contacts → teachers
ALTER TABLE employee_emergency_contacts
  DROP CONSTRAINT IF EXISTS employee_emergency_contacts_teacher_id_fkey;

ALTER TABLE employee_emergency_contacts
  ADD CONSTRAINT employee_emergency_contacts_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES teachers(id)
  ON DELETE CASCADE;

-- Fix employee_edit_history → teachers
ALTER TABLE employee_edit_history
  DROP CONSTRAINT IF EXISTS employee_edit_history_teacher_id_fkey;

ALTER TABLE employee_edit_history
  ADD CONSTRAINT employee_edit_history_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES teachers(id)
  ON DELETE CASCADE;









-- Fix teacher_class_assignments → employees
ALTER TABLE teacher_class_assignments
  DROP CONSTRAINT IF EXISTS teacher_class_assignments_teacher_id_fkey;

ALTER TABLE teacher_class_assignments
  ADD CONSTRAINT teacher_class_assignments_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix teacher_leaves → employees
ALTER TABLE teacher_leaves
  DROP CONSTRAINT IF EXISTS teacher_leaves_teacher_id_fkey;

ALTER TABLE teacher_leaves
  ADD CONSTRAINT teacher_leaves_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix student_remarks → employees
ALTER TABLE student_remarks
  DROP CONSTRAINT IF EXISTS student_remarks_teacher_id_fkey;

ALTER TABLE student_remarks
  ADD CONSTRAINT student_remarks_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix timetable → employees
ALTER TABLE timetable
  DROP CONSTRAINT IF EXISTS timetable_teacher_id_fkey;

ALTER TABLE timetable
  ADD CONSTRAINT timetable_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix salary_records → employees
ALTER TABLE salary_records
  DROP CONSTRAINT IF EXISTS salary_records_teacher_id_fkey;

ALTER TABLE salary_records
  ADD CONSTRAINT salary_records_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix teacher_attendance → employees
ALTER TABLE teacher_attendance
  DROP CONSTRAINT IF EXISTS teacher_attendance_teacher_id_fkey;

ALTER TABLE teacher_attendance
  ADD CONSTRAINT teacher_attendance_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix teacher_documents → employees
ALTER TABLE teacher_documents
  DROP CONSTRAINT IF EXISTS teacher_documents_teacher_id_fkey;

ALTER TABLE teacher_documents
  ADD CONSTRAINT teacher_documents_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;

-- Fix teacher_permissions → employees
ALTER TABLE teacher_permissions
  DROP CONSTRAINT IF EXISTS teacher_permissions_teacher_id_fkey;

ALTER TABLE teacher_permissions
  ADD CONSTRAINT teacher_permissions_teacher_id_fkey
  FOREIGN KEY (teacher_id)
  REFERENCES employees(id)
  ON DELETE SET NULL;



  VERIFY after running the SQL:
sql-- Try to find any remaining RESTRICT foreign keys on teachers or employees
SELECT
  tc.table_name,
  kcu.column_name,
  ccu.table_name AS references,
  rc.delete_rule
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu
  ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage ccu
  ON ccu.constraint_name = tc.constraint_name
JOIN information_schema.referential_constraints rc
  ON rc.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY'
AND ccu.table_name IN ('teachers', 'employees')
AND rc.delete_rule = 'NO ACTION'
ORDER BY tc.table_name;